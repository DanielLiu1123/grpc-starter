dependencies {
    api(project(":grpc-starters:grpc-starter-validation"))
}

apply from: "${rootDir}/protobuf.gradle"

sourceSets {
    main {
        proto {
            srcDir "proto"
        }
    }
}
