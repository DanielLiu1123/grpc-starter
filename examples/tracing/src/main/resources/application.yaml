spring:
  application:
    name: metrics-example-app

grpc:
  server:
    reflection:
      enabled: true
  client:
    authority: localhost:${grpc.server.port:9090}

management:
  otlp:
    tracing:
      endpoint: http://localhost:4317
  tracing:
    sampling:
      probability: 1.0
